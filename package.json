{
  "name": "gfk-transport-website",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing and lead-generation website for GFK Transport LLC",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "images": "node tools/make-images.mjs",
    "dev": "node build.mjs --watch --serve",
    "serve": "node build.mjs --serve",
    "check": "node build.mjs --check"
  },
  "engines": {
    "node": ">=18"
  }
}
